# Frontend environment variables — copy to frontend/.env for local dev.
# Vite only exposes vars prefixed with VITE_.

# Backend base URL. Leave unset for local dev (defaults to http://localhost:3000).
# In production set it to your deployed backend, e.g.:
# VITE_API_URL="https://whiteboard-backend.onrender.com"
